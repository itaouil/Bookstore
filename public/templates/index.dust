{>"layouts/master" /}

{<body}

  {#books}
    <div class="large-3 columns books end">
      <img src="{.cover}">
      <h4>{.title}</p>
      <p>{.truncText}</p>
      <div class="price">Buy it for <span>£{.price}</span></div>
      <br />
      <a href="/books/details/{._id}" class="button small">Book Details</a>
    </div>
  {/books}

{/body}
